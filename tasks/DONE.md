# Done

Pre-work completed before the task board (not one of the 18 implementation
tasks):

- **Rule bundle** — 3 upstream YARA repos cloned, compile-tested per file under
  yara-x, FP-pruned, deduped, merged. Result: 875 files / 13,134 rules at
  `signatures/rules/bundled.yar` (+ `.yarc`). License NOTICE + MANIFEST +
  per-repo licenses in `signatures/`. Reproducibility captured as TASK-018.

_No implementation tasks done yet._
